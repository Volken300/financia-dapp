// This is the address of the deployed Financia smart contract on Sepolia testnet
// Use environment variable if available, otherwise fallback to the default address
export const CONTRACT_ADDRESS = process.env.REACT_APP_CONTRACT_ADDRESS || '0x123456789abcdef123456789abcdef123456789a';

// Note: For production deployment, set the REACT_APP_CONTRACT_ADDRESS environment variable
// in your AWS deployment environment